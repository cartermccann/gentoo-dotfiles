gui-wm/mangowm ~amd64
gui-libs/scenefx ~amd64
x11-terms/ghostty ~amd64
gui-apps/swaync ~amd64
x11-misc/ly ~amd64
media-fonts/nerdfonts ~amd64
x11-themes/bibata-xcursors ~amd64
app-misc/cliphist ~amd64
# bootloader — see system/kernel/postinst.d/95-limine.install
sys-boot/limine ~amd64
# pre-seeded so autounmask doesn't have to write them mid-run (which leaves
# ._cfg files behind and stalls the batch)
=x11-terms/ghostty-terminfo-1.3.1 ~amd64
=dev-lang/zig-bin-0.15.2 ~amd64
=app-misc/brightnessctl-0.5.1 ~amd64
=gui-apps/wtype-0.4 ~amd64
=gui-apps/wlsunset-0.4.0 ~amd64

# ── dev tooling ────────────────────────────────────────────────
# GURU keeps everything at ~amd64 as a matter of policy — nothing in the
# overlay is ever stabilised, so any package from it needs a line here.
# (app-misc/tmux is NOT listed: 3.5a is stable amd64 in ::gentoo.)
app-shells/direnv ~amd64
app-misc/television ~amd64
