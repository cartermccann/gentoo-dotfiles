# Alacritty — colors come from the active theme (atlas-theme).
[general]
import = ["~/.config/alacritty/theme.toml"]

[window]
opacity = 0.95
padding = { x = 8, y = 8 }
decorations = "none"

[font]
size = 11.0
normal = { family = "JetBrainsMono Nerd Font", style = "Regular" }
